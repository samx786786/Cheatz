<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cheatz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cheatz">
        <activity android:name=".ImportantActivity"></activity>
        <activity android:name=".RecycleActivity" />
        <activity android:name=".Workflow" />
        <activity android:name=".Searchengine" />
        <activity android:name=".Zoomimage" />
        <activity android:name=".ProfileSavedataActivity" />
        <activity android:name=".Lab" />
        <activity android:name=".Questionbank" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".Mainpage" />
        <activity android:name=".Homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>